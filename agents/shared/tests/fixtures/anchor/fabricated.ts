// fabricated.ts -- 0076 fixture: the ONLY status that may demote. The quote
// the model produced for this file exists in NO file of the batch; it was
// invented outright. Replaces the draft's gone.ts (review recall-7).
export function totalOf(items: Item[]) {
  return items.reduce((acc, it) => acc + it.price, 0);
}
